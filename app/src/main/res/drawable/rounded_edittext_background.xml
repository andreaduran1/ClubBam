<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp" /> <!-- Cambia 8dp por el radio deseado -->
    <solid android:color="@color/colorOnError" /> <!-- Color de fondo del campo de texto -->
    <stroke
        android:width="1dp"
        android:color="@color/colorSecondary"/>
</shape>